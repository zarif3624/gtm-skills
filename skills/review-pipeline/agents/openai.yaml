interface:
  display_name: "Review Pipeline"
  short_description: "Find risks and actions across the pipeline"
  default_prompt: "Use $review-pipeline to review this pipeline and prioritize actions."
